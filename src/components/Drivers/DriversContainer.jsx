import React from 'react';
import {Drivers} from './Drivers';

export const DriversContainer = (props) => {

    return (
      <div className="page-container">
        <Drivers/>
      </div>
    );

}
